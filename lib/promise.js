/**
 * Promises states according to the A+ Specification
 * @readOnly
 * @enum {number}
 */
var State = {
  PENDING: 0,
  FULFILLED: 1,
  REJECTED: 2
};

var Promise = module.exports = function() {};
